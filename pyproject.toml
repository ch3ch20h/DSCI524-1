[tool.poetry]
name = "my_pack524"
version = "0.1.0"
description = "A sample Python package"
authors = ["Yichun Liu <yichunl@umich.edu>"]

[tool.poetry.dependencies]
python = "^3.9"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
